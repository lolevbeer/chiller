_metadata:
  major_version: 1
display_information:
  name: Chiller
  description: Chiller status and history, plus datalogger re-arm
  background_color: "#111827"
features:
  bot_user:
    display_name: Chiller
    always_online: false
  slash_commands:
    - command: /chiller
      description: Chiller status, alarms, trends, and runtimes; re-arm the datalogger
      usage_hint: status | alarms | trend 6h | circuit a | runtimes | why | rearm
      should_escape: false
oauth_config:
  scopes:
    bot:
      - commands
settings:
  org_deploy_enabled: false
  socket_mode_enabled: true
  is_hosted: false
  token_rotation_enabled: false
