# Claude instructions

- NEVER run the server (`node chiller_dashboard.js`, `npm start`, or any variant). The user runs it externally. Assume it is already running at http://localhost:8000 when you need to check it.
